'use client';

// ReceiptModal is no longer used in the new POS page.
// The receipt modal is now built directly into /app/(app)/pos/page.tsx
export default function ReceiptModal() {
  return null;
}
