'use client';

// ProductGrid is no longer used in the new POS page.
// Kept for compatibility — the new POS is fully self-contained in /app/(app)/pos/page.tsx
export default function ProductGrid() {
  return null;
}
